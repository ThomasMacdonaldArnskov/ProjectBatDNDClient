package commons.transfer.objects;

import commons.transfer.TransferableObject;
import commons.transfer.Transferables;

public class SimpleTransfer extends TransferableObject {
    public SimpleTransfer(Transferables transferables) {
        super(transferables);
    }
}
